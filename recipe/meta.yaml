{% set name = "yourdfpy" %}
{% set version = "0.0.58" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/yourdfpy-{{ version }}.tar.gz
  sha256: 68d23688802c8e23843a8981f828bcca9a7875cdb02be7f3a0e67b42929727f3

build:
  noarch: python
  entry_points:
    - yourdfpy = yourdfpy.viz:run
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=46.1.0
    - setuptools-scm >=5
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - importlib-metadata
    - lxml
    - trimesh >=3.11.2
    - numpy
    - six

test:
  imports:
    - yourdfpy
  commands:
    - pip check
    - yourdfpy --help
  requires:
    - pip
    - setuptools
    - python {{ python_min }}

about:
  home: https://github.com/clemense/yourdfpy/
  summary: A simpler and easier-to-use library for loading, manipulating, saving, and visualizing URDF files.
  dev_url: https://github.com/clemense/yourdfpy/
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - ruben-arts
